prefix: default
device: cuda
mode: train
log_dir: logs/${mode}
eval_data_path: null
use_wandb: true
seed: 0

agent:
  agent_name: dp

env:
  env_class: ???
  num_eef: 2
  dof: 7
  eef_dim: 13
  vectorize: false
  args:
    num_eef: ${env.num_eef}
    dof: ${env.dof}
    seed: ${seed}
    obs_mode: "state"
    ac_mode: "rel"
    max_episode_length: ???
    num_points: ${data.dataset.num_points}
    randomize_rotation: false
    randomize_scale: false
    scale_low: 1.0
    scale_high: 1.0
    scale_aspect_limit: 100.0
    uniform_scaling: false

data:
  dataset_class: base_dataset
  dataset:
    num_training_steps: ???
    path: null
    num_points: 1024
    num_augment: 0
    same_aug_per_sample: true
    aug_keep_original: true
    aug_scale_low: 0.5
    aug_scale_high: 1.5
    aug_scale_aspect_limit: 1.0
    aug_scale_rot: -1
    aug_scale_pos: 0.1
    aug_zero_z_offset: false
    aug_center: [0., 0., 0.]
    shuffle_pc: true
    num_workers: 12
    dof: ${env.dof}
    num_eef: ${env.num_eef}
    eef_dim: ${env.eef_dim}
    obs_horizon: ${model.obs_horizon}
    pred_horizon: ${model.pred_horizon}
    reduce_horizon_dim: false
    min_demo_length: 15

model:
  hidden_dim: 32
  noise_scheduler:
    _target_: diffusers.schedulers.scheduling_ddpm.DDPMScheduler
    num_train_timesteps: 100
    beta_schedule: squaredcos_cap_v2
    clip_sample: true
    prediction_type: epsilon
  obs_horizon: 4
  ac_horizon: 1
  pred_horizon: 32
  encoder:
    c_dim: ${model.hidden_dim}
    backbone_type: vn_pointnet
    backbone_args:
      h_dim: ${model.hidden_dim}
      c_dim: ${model.encoder.c_dim}
      num_layers: 4
      knn: 8
  obs_mode: ${env.args.obs_mode}
  ac_mode: ${env.args.ac_mode}
  use_torch_compile: True # was false

training:
  batch_size: 4096 # for og dp on rtx a4500 20gb vram 480p dataset, was 32 originally
  num_epochs: 200
  lr: 3e-5
  weight_decay: 1e-6
  num_eval_episodes: 12
  eval_interval: 1000000 # do not eval during training
  save_interval: 50 # was 50
  vis_interval: 100 # 100
  # best: 10
  ckpt: null

eval:
  last_ckpt: 1999
  num_ckpts_to_eval: 5

hydra:
  run:
    dir: ${log_dir}/${prefix}

wandb:
  entity: ab36
  project: equibot

franka_rope:
  physics_dt: 1.0/60.0
  rendering_dt: 1.0/60.0
  _randomize: True
  _randomize_on_reset: False
  extra_scenes: null #null #[fixed_cylinder] # list, possible values: fixed_cylinder, visual_line
  _rope_damping: 0.6
  _rope_stiffness: 0.1
  from_demo: r"C:\Users\Shadow\project\franka_rope\demos\test_straighten_right\output_data_2024-12-02_15-38-13.json"
  start_time: 350
  manually_close: False
  seed: 42